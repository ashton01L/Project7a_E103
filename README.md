Define a function named 'square_list' that takes as a
parameter a list of numbers and replaces each value with the
square of that value. It should not return anything
it should mutate the original list.